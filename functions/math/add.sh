
# _.add(augend, addend)

# Adds two numbers.

# Arguments
# augend (number): The first number to add.
# addend (number): The second number to add.
# Returns
# (number): Returns the sum.

# Example
# _.add(6, 4);
# // → 10

function _.add {
  _._math "$1 + $2"
}

