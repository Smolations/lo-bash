#!/bin/bash

h1 'isBoolean'

# this may be an unusable function...

# _.isBoolean true && pass || fail
# _.isBoolean false && pass || fail

# foo=true
# _.isBoolean $foo && pass || fail

# _.isBoolean "false" && fail || pass

